# Shoujian Oracle working agreements

- 本项目是独立的轻量虚拟卦师与经传 RAG 教学组件，保留虚拟人主持、普通自由对话、经传知识问答、公开问界与基础排卦；只有用户明确选择时才进入起卦。冻结经传数据来自公开来源，不复制或依赖主项目的住宅知识、纳甲时证、案卷、支付或身份系统。
- 起卦计算必须是可测试的纯函数；问题文字不得改变卦象结果。
- 默认无密钥时不联网；启用 Gemini 后，只把用户主动提交的文字、短录音、最近上下文、必要卦象证据及单独同意的四柱符号发往 Google。原始生日与时刻仅在页面内存，刷新清除，不得持久化或随请求上传。最近 24 条已完成对话、阶段、原问和六爻快照只存浏览器本机；恢复卦象必须重新走确定性计算，服务端不得建立用户档案。
- 日期与时间回答必须使用服务端注入的 `Asia/Shanghai` 时钟；自由回答通过 SSE 流式传输，前端在同一条消息内渐进显示。
- TTS 不得占用文字会话的忙碌状态；首个完整句应立即进入最多两句预取的顺序播放队列，文字完成后必须立即允许下一轮输入。新问题、停止回答或关闭语音必须淘汰旧请求和旧音频。语音输入优先实时增量转写，并保留可取消和 Gemini 兼容兜底。
- 经传回答必须先检索 `knowledge/shoujian-rag.v1.json`，公开本轮片段和源链接；不得用模型记忆补造原文、出处或未检索证据。
- 医疗、法律、投资、生意、感情等主题都允许作传统文化解释，但只给观察角度；不得输出停药、买卖、诉讼等专业指令，不得承诺收益、吉凶、生死、灾祸、应期或确定结果。只有即时伤害自己或他人的问题停止起卦并转现实危机支持。
- 行为变更应补测试；提交前运行 `npm test` 和 `npm run check`。
