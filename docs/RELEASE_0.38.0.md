# 守简 0.38.0 第十一轮自检

本轮复查连续语音“打断旧回答后立刻说新问题”的时序。此前控制器会立即倾听和提交新问题；若旧的网络回答尚未在页面侧完成取消收束，`sendText` 因忙碌状态直接返回，新问题会静默丢失。

现在新语音仍可立即开始识别，但提交会等旧提交结束。等待期间若用户再次打断或退出，旧轮次的检查会阻止过期问题提交。新增定向回归复现了旧时序并验证修复。

本地 `npm test` 224 / 224、Chromium / WebKit 及两组移动仿真 44 / 44 通过；固定 RAG 集 recall@4 与域外负例精度均为 100%，项目检查、空白符检查和生产依赖审计通过。真实麦克风、打断体感和回声环境仍需设备验收。
