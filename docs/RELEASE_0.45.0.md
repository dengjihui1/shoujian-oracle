# 守简 0.45.0 第十八轮自检

聊天供应商熔断器此前在所有供应商均处于冷却期时，会强行返回主供应商再试一次。持续故障或额度耗尽时，这条路径会重复触发失败的云端请求。现在全部熔断时直接返回可重试的 503；冷却结束后恢复正常尝试。流式与非流式路径均覆盖，语音服务不受聊天熔断器控制。

定向回归模拟所有供应商失败，确认冷却期内两种聊天模式均不再调用上游，并在冷却结束后恢复调用。真实供应商额度、重试费用、告警及账单阈值仍需生产环境验收。

本地 `npm test` 239 / 239、Chromium / WebKit 及两组移动仿真 44 / 44 通过；固定 RAG 集 recall@4 与域外负例精度均为 100%，项目检查、空白符检查和生产依赖审计通过。
