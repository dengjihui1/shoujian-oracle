# 守简 0.29.0 第二轮自检

本轮继续处理 0.28.0 评估里“语音异常恢复”和“卦后普通问题自由度”。0.28.0 的总体产品边界、RAG 数据范围、真实设备与收费服务缺口仍见 `ASSESSMENT_2026-09-23.md`；本文件不把尚未验证的项目写成已完成。

## 已复现并修复

1. **WAV Blob 播放可能卡住取消**：Web Audio 不可用时，旧代码在 `audio.play()` Promise 完成后才返回播放器句柄。某些浏览器若让这个 Promise 悬置，用户即使中断也无法及时暂停和释放 Blob。现在立即返回句柄，后台观察播放承诺；`stop()` 可先行暂停并释放对象 URL。播放过程中发生 `error` 也会向队列报告，而不是当作正常结束。
2. **浏览器朗读启动后可能永不结束**：已有 5 秒“未启动”看门狗；新增与句长相关、上限 60 秒的播放看门狗。`onend`、主动停止与超时都会清除定时器；超时后取消朗读、让文字对话保持可用。
3. **卦后普通聊天仍被旧卦污染**：此前 `purpose=chat` 虽不强制 RAG，却仍把冻结原问与旧卦放进系统提示词。现在只有明确的卦象解读轮次才给模型这些上下文；普通聊天保留用户可见的最近完成对话，但不额外把旧卦当作本轮事实强塞进去。问“基金是什么”“太阳为什么发光”不会因为刚起卦而自动转回旧卦；明确问“这卦”仍走解卦路线。

## 验证

- `npm test`：211 / 211 通过；新增 HTML 音频悬置、播放错误、朗读启动后超时、卦后普通聊天上下文隔离等回归。
- Playwright：本机 Chromium、WebKit、Pixel 7 与 iPhone 14 仿真 24 / 24 通过；这些测试模拟外部 API，不代表真实语音供应商速度。
- 本机 Firefox：进一步直接运行 `firefox.exe --version` 也无法启动；Windows Application 的 SideBySide 事件指出 `mozglue` 程序集加载错误。因此它是本机 Playwright Firefox 安装/运行环境故障，**Firefox 页面行为仍未验收**。不要用 24 / 24 代替完整 30 / 30。
- `npm run eval:rag`：固定集 recall@4 与负例精度均为 100%，本轮均值 5.03 ms、P95 10.81 ms；这是检索评测，不是模型回答评分。`npm run check`、`git diff --check` 通过；`npm audit --omit=dev --audit-level=low` 报告 0 个已知漏洞。
- GitHub：0.28.0 与 0.29.0 的提交都已推送；[Actions 35853375739](https://github.com/dengjihui1/shoujian-oracle/actions/runs/35853375739) 对 `b2f8051` 返回 `success`，覆盖仓库配置的 30 条浏览器流程和其他发布门禁。本机 Firefox 故障仍应单独修复，不把远端 Linux 成功当成本机 Windows 验收。

## 还不能宣称完成

- 真实麦克风连续三轮、噪声/回声、真实打断与浏览器音色体验；
- Google Cloud TTS 新句首声 P50/P95；浏览器 `onstart` 指标也不等同声学实测；
- 真人对“老卦师”口吻、引用贴合度、自然对话和动作可信度的盲评；
- 正式域名长期运行、账号/权益/成本配额与按次收费。

当前可接受范围仍是可审查的本地开源演示。下一轮最有价值的是在真实设备上运行 `DEVICE_ACCEPTANCE.md`，并建立人工标注的回答质量样本；不要用再增加几个模拟单测代替这两项外部验收。
