# 守简 0.33.0 第六轮自检

本轮处理浏览器语音识别器重复报告同一份结果的情况。即使 0.31.0 已避免把文字重复追加，相同回调仍会反复通知连续语音状态机，重启停顿自动送问计时器。

识别器现在比较完整 final / interim 快照；文字没有变化时不再发出 `onText`。新增跨识别器与连续语音状态机的测试，确认三次相同结果只建立一个自动提交计时器，最终问题只发送一次。

本轮只改语音识别事件去重，不改经传知识、起卦计算、模型与供应商配置。本地 `npm test` 219 / 219 通过；Chromium、WebKit 及两组移动仿真 40 / 40 条浏览器流程通过。固定 RAG 集 recall@4 和域外负例精度均为 100%；`npm run check`、`git diff --check` 和生产依赖审计通过。GitHub CI 仍须按最终提交核对。

真实麦克风连续对话与延迟仍按 [设备验收清单](DEVICE_ACCEPTANCE.md) 验收。
