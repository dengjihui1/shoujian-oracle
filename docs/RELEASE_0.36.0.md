# 守简 0.36.0 第九轮自检

本轮复查静态文件服务。请求测试证实服务端此前会将 `server/index.mjs` 等仓库文件直接作为静态资源返回。现在静态路径只允许入口 HTML、`src/` 的前端模块和 `assets/` 的页面图片；服务端代码、依赖目录、知识包原始文件及包配置不能再从网页路径下载。无法解码的 URL 路径返回 400。

新增请求级回归，覆盖页面正常加载与这些内部路径的 404。本地 `npm test` 222 / 222、Chromium / WebKit 及两组移动仿真 44 / 44 通过；固定 RAG 集 recall@4 与域外负例精度均为 100%，项目检查、空白符检查和生产依赖审计通过。此轮验证的是 HTTP 暴露范围；真实设备语音与真人回答质量仍需单独验收。
