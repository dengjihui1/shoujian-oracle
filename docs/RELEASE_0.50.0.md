# 守简 0.50.0 第二十三轮自检

逐模块复核 P01 与 P06 时复现：组件卸载后立即重挂，新 `/api/status` 已返回在线，旧请求随后返回离线，会把页面错误地改回本地模式。组件现在取消卸载前的状态请求，以代数标识丢弃过期结果；P06 的状态接口接受取消信号。

浏览器回归先在旧代码失败，修复后本机 Chromium、WebKit 的桌面和移动项目 4/4 通过。用户输入、会话记忆和对话请求不因状态检查变化而改变。

本地验证：`npm test` 245/245、`npm run check`、`npm run eval:rag`、`git diff --check` 均通过。完整跨浏览器与容器门禁由远端 CI 验证。真实设备语音与正式部署环境不由该回归覆盖。
