{
  "name": "shoujian-oracle",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "A selective extraction of a virtual host, bounded dialogue, question guard, and deterministic divination engine.",
  "scripts": {
    "start": "node server/index.mjs",
    "test": "node --test",
    "check": "node scripts/check-project.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
