# Keep DOMAIN and APP_ENV_FILE in your existing compose.env.
# Add these two lines there after generating a real password hash.
# Single quotes prevent Compose from expanding the bcrypt hash's dollar signs.
INVITE_USER=
INVITE_PASSWORD_HASH=''
