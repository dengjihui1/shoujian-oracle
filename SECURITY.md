# Security

守简在未配置密钥时不联网、不保存输入。启用 Gemini 后，密钥只由本机 Node 服务读取；静态服务拒绝点文件访问，真实 `.env` 也被 Git 忽略。用户主动提交的文字、短录音、必要卦象上下文和本轮最多 8 条公开经传检索片段会发送给 Google；转写完成后，服务会尽力立即删除 Files API 中的临时音频。

RAG 知识包在仓库中公开、只读且带来源。服务端只接受检索器返回的白名单片段，并拒绝模型对本轮未检索源号的引用；这能减少伪造出处，但不能保证模型解释本身正确，界面必须继续让用户查看原文。

公开部署不能直接照搬本机配置。请至少增加身份认证、持久化限流、HTTPS、账单告警、密钥托管和日志脱敏，不要让匿名用户共享个人 API 密钥。

请通过 GitHub Security Advisory 私下报告安全问题，不要在公开 Issue 中粘贴个人问题、凭据或其他敏感信息。项目不会通过聊天或 Issue 索取密码、验证码、支付密钥或身份证明。
